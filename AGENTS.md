# AGENTS.md

本文件用于指导 AI 将 Word 论文迁移为本仓库的南京邮电大学本科毕业设计（论文）LaTeX 版本。目标是忠实迁移内容，并让输出 PDF 符合当前模板格式。

## 核心原则

- 不润色、不改写、不删减论文正文、摘要、关键词、图题、表题、参考文献题名等可见文本。
- 只在 LaTeX 结构、命令、转义字符、图片路径、表格环境、引用命令等排版实现层面做必要转换。
- 以 Word 论文的最终修改版为内容来源，以本仓库 `main.tex`、`njupthesis.cls`、`reference.bib` 为 LaTeX 目标结构。
- 不把参考文献条目直接写在 `main.tex` 中；参考文献必须进入 `reference.bib`，正文只保留 `\cite{...}`。
- 不随意修改 `njupthesis.cls`。只有当 Word 标准模板与当前类文件存在明确格式差异，且无法通过 `main.tex` 正常表达时，才修改类文件。
- 遇到无法可靠判断的内容，记录在迁移说明或待确认清单中，不要把说明文字插入论文正文。

## 输入检查

开始迁移前先确认以下文件：

- Word 论文最终稿：优先使用用户给出的 `.docx`；如果是 `.doc`，先用可靠工具转换为 `.docx` 或 PDF 后再读取格式。
- Word 标准模板：用于核对封面、摘要、目录、正文标题、图表、参考文献、附录等格式。
- LaTeX 模板：本仓库当前版本，不要使用上游旧模板替代。

读取 Word 时同时关注文本和结构：

- 封面字段：题名、作者、学院、专业、班级、学号、指导教师、起止日期、原创性声明日期、签名。
- 摘要：中文摘要、中文关键词、英文 ABSTRACT、英文 Key words。
- 正文层级：章、节、小节，以及正文中使用的编号段落。
- 图、表、公式：编号、标题、正文引用位置、图片源、表格内容、公式编号。
- 参考文献：正文引用顺序、文献条目类型、作者、题名、期刊/会议/出版信息、年份、页码、URL、访问日期。
- 附录和成果：是否存在附录、学术成果、获奖情况等模板尾部内容。

## LaTeX 迁移规则

### 基本信息

将封面信息写入 `main.tex` 开头的模板命令：

- `\title{...}`
- `\author{...}`
- `\advisor{...}`
- `\school{...}`
- `\major{...}`
- `\studentclass{...}`
- `\studentid{...}`
- `\graduateyear{...}`
- `\begindate{...}`
- `\finishdate{...}`
- `\createdate{年}{月}{日}`

标题过长时允许在 `\title{...}` 内使用显式换行控制，但不要缩小标题字号，不要改标题文字。

### 摘要和关键词

- 中文摘要放入 `\begin{chineseabstract}` 与 `\end{chineseabstract}`。
- 中文关键词使用 `\chinesekeyword{...}`。
- 英文摘要放入 `\begin{englishabstract}` 与 `\end{englishabstract}`。
- 英文关键词使用 `\englishkeyword{...}`。
- 关键词内容保持 Word 原文和分隔符，不额外空一行。
- 摘要跨页时不得出现正文页眉页脚或阿拉伯页码。

### 正文结构

- 正文开始前保留 `\thesistableofcontents` 和 `\thesischapterexordium`。
- 一级标题使用 `\chapter{...}`。
- 二级标题使用 `\section{...}`。
- 三级标题使用 `\subsection{...}`。
- 不建议使用四级标题；Word 中更深层级标题优先按原文编号写为普通正文段落。
- 正文段落只用空行分段，不为了视觉效果插入多余空行或手工空格。

### 图片

- 从 Word 中提取图片原图，放入 `pic/`。
- 图片文件名使用稳定、可读、无空格的名称，例如 `fig-chapter2-architecture.png`。
- 用 `figure` 环境、`\includegraphics`、`\caption{...}` 和 `\label{...}` 表达图片。
- 图题文字必须与 Word 保持一致。
- 正文中的“如图...”引用应替换为 `图\ref{...}`；不要改变句子其余文字。

### 表格

- 表格优先使用三线表，不使用 Word 网格线样式。
- 常规表格使用 `table`、`tabularx` 或 `tabular`，配合 `booktabs` 的 `\toprule`、`\midrule`、`\bottomrule`。
- 表题文字必须与 Word 保持一致，放在表格上方。
- 单位优先放入表头，例如 `耗时 / ms`。
- 表格尽量不要跨页；如果确实必须跨页，拆分为续表，并在续表标题中保留原表编号和“续表”语义。
- 不为塞入页面而缩小到难以阅读的字号；优先调整列宽、换行或拆表。

### 公式

- 独立公式使用 `equation` 或合适的数学环境。
- 保持 Word 中公式含义和编号引用关系。
- 正文引用公式时使用 `式\ref{...}` 或与原文一致的表达。

### 参考文献

- 正文引用使用 `\cite{...}`。
- 文献条目写入 `reference.bib`，不要写进 `main.tex`。
- 保留模板的 `\thesisreference`，让模板和 BibTeX 自动生成参考文献列表。
- 文献格式沿用本模板已有的 GBT-7714/BibTeX 设置，不手写编号列表。
- 如果 Word 只有纯文本参考文献，先尽量转换为 BibTeX；缺失字段不要编造，记录为待确认。

### 附录和尾部

- 结束语使用 `\thesisconclusion`。
- 致谢使用 `\thesisacknowledgement`。
- 附录使用 `\thesisappendix`，多个附录按模板自动编号。
- 本科期间学术成果、获奖情况等内容按 Word 原文迁移，不额外添加宣传性文字。

## 格式核查清单

迁移后必须对照 Word 标准模板和最终 Word 论文核查：

- 封面字段完整，封面信息栏无表格边线，字体和换行符合模板。
- 原创性声明日期和签名位置正常。
- 中文摘要标题、正文、关键词格式正确；关键词与摘要正文之间不额外空行。
- 英文 ABSTRACT 标题、正文、Key words 格式正确；Key words 与摘要正文之间不额外空行。
- 摘要第二页、ABSTRACT 第二页、目录页不得出现正文页码或正文页眉。
- 正文从第一章开始使用阿拉伯页码和页眉页脚。
- 章、节、小节标题字号、字体、间距与标准模板一致。
- 图题在图下方，表题在表上方，图表编号和正文引用一致。
- 表格为三线表，未发生不必要跨页；跨页表已拆为续表。
- 参考文献由 BibTeX 生成，格式不是 `main.tex` 中的手写列表。

## 编译和验证

本模板使用 XeLaTeX 和 BibTeX。迁移完成后在仓库根目录运行：

```bash
xelatex main
bibtex main
xelatex main
xelatex main
```

验证时至少检查：

- 编译命令退出码为 0。
- 日志中没有未定义引用、未定义 citation、缺失图片、缺失字体等关键错误。
- 生成的 `main.pdf` 能打开，封面、摘要、目录、正文、参考文献、附录顺序正确。
- 对照 Word 最终稿抽查每一章开头、每个图表、每条参考文献，确认没有文本遗漏。

## 交付要求

- 保持改动集中在 `main.tex`、`reference.bib`、`pic/` 以及必要的模板文件中。
- 不提交临时编译产物，除非用户明确要求提交 PDF。
- 最终说明中列出迁移过的主要内容、无法自动确认的项目、编译验证结果。
